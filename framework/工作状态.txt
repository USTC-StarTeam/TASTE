# TASTE framework_dev 工作状态

## 2026-06-19 初始状态
- 已在远程 `/home/fmh/workspace/TASTE` 内从 `framework/` 复制出 `framework_dev/`，后续只修改 `framework_dev/`。
- 已清理副本中的 `__pycache__` 和 `.pyc`，避免把运行缓存当作框架源码。
- 已把 `framework_dev/` 写入本仓库本地 `.git/info/exclude`，防止开发副本和中间产物进入 git 跟踪候选。
- 当前目标：把 `framework_dev` 维护成独立的 TASTE 后端编排框架，只负责科研流程规划、状态产物、模块调用契约和 Claude Code 自主决策，不实现或替代七个模块本身，也不耦合 Web 前端。
- 环境约束：管理环境使用 `/home/fmh/workspace/miniforge/envs/ar_taste/bin/python3.11`；`rg` 使用同一 conda 环境；必要时再显式加载 `/home/fmh/workspace/.nvm/nvm.sh`。


## 2026-06-19T07:12:57.760490+00:00 - framework_dev 独立后端编排器
- 已新增 `scripts/taste_backend/` 后端编排包，按 common、contracts、runtime、orchestration、status、validation 六类组织；外层只保留 `scripts/orchestration/run_taste_framework.py`、`scripts/contracts/print_module_contracts.py`、`scripts/validation/audit_framework_backend.py（旧 audit_framework_dev_backend.py 仅作兼容 wrapper）` 三个薄 CLI 入口。
- 新后端只通过 `modules/<stage>/main.py --contract` 和 `--action` 调用七个模块，不实现模块内部功能，不依赖 Web 前端；默认 `dry-run` 只在 `framework_dev/workspace/runs/<run_id>/` 内写命令计划、状态、日志和前端可读 JSON。
- 已实现三种决策策略：`deterministic` 按 Finding -> Reading -> Ideation -> Planning -> Environment -> Experimenting -> Writing 顺序推进；`hybrid` 优先让 Claude Code 返回 JSON 决策，失败后回退确定性流程；`claude` 必须由 Claude Code 决策。
- 已实现前端可读产物：`public/frontend_status.json`；人类可读产物：`public/workflow_status.md`；完整状态：`state/workflow_state.json`；命令回执和日志：`logs/*.receipt.json`、`logs/*.stdout.log`、`logs/*.stderr.log`。
- 已新增 `resources/README.md`，说明 resources 只保存框架模板和 Claude 使用约束，不保存七模块私有实现、真实数据、密钥、训练日志或项目科学证据。
- 已新增 `framework_dev/tests/test_taste_backend.py`，覆盖七模块顺序、静态契约、dry-run 前端状态和状态目录隔离。
- 验证通过：`compileall`；七模块 `contracts` 命令返回 7 个模块；`verify_dry_run_v2` dry-run 状态为 `paper_pipeline_finished` 且进度 7/7；`verify_dry_run_gates` 生成 13 条记录，其中 6 条 gate；`audit_framework_backend.py --run-id verify_isolation` 返回 pass；`pytest framework_dev/tests -q` 通过 4 项。
- 注意：本轮没有修改原 `framework/`、`modules/`、`web/`。`framework_dev/` 已在 `.git/info/exclude` 中，本副本和中间产物不会进入 git 跟踪候选。

- README 测试命令已修正为 `pytest ... -o cache_dir=framework_dev/workspace/pytest_cache`，避免后续测试缓存写到仓库根目录。


## 2026-06-19T07:14:09.007170+00:00 - 最终验证
- 最终验证命令已使用 `/home/fmh/workspace/miniforge/envs/ar_taste/bin/python3.11` 和 `ar_taste` 环境执行。
- `compileall` 通过；七模块 `contracts` 输出 `module_count=7`；`final_dry_run` 状态为 `paper_pipeline_finished` 且 7/7 完成；`final_dry_run_gates` 含 6 条 gate 记录；`final_isolation` 隔离审计为 pass；`pytest framework_dev/tests -q -o cache_dir=framework_dev/workspace/pytest_cache` 通过 4 项。
- 验证后已删除 `framework_dev` 内 `__pycache__` 和 `.pyc/.pyo`。
- `git status --short -- framework_dev` 为空，说明 `framework_dev/` 未进入 git 跟踪候选。
- `git status --short -- framework` 仍显示 `M framework/scripts/auto_research/module_boundaries.py`，这是开始前就存在的原框架改动，本轮没有修改原 `framework/`。
