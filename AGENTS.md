# AGENTS.md

## Development Environment

- If available, use the conda environment named `TASTE` for local development and verification.
- Prefer existing project commands and scripts before introducing new tooling.
- Do not commit local environment directories, caches, run outputs, secrets, or downloaded papers.
