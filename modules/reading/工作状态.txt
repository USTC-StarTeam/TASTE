# Reading 模块工作状态

更新时间：2026-06-18
工作目录：`/home/fmh/workspace/TASTE/modules/reading`
远程主机：`hidimension_5090_1`

## 本次较大修改

1. 将 `scripts/` 下原有大脚本按功能移动到子目录：
   - `scripts/pipeline/read_pipeline.py`
   - `scripts/orchestration/ensure_current_find_research_plan.py`
   - `scripts/repair/repair_current_find_full_text_evidence.py`
2. 保留根目录同名兼容薄壳，避免旧代码继续 `import read_pipeline` 或直接执行旧脚本路径时失效。
3. 新增独立单篇论文精读流水线：`scripts/pipeline/standalone_deep_read.py`。
4. 新增资料获取层：`scripts/acquisition/paper_sources.py`，复用旧 `read_pipeline.py` 的 PDF 候选、下载和正文抽取函数。
5. 新增 Claude/subagent 编排层：`scripts/orchestration/claude_subagent.py`，生成中文 prompt，并要求主控 Claude Code 必须调用 Task/subagent 做全文精读。
6. `main.py` 新增 `deep-read` / `deep_read` / `standalone` / `standalone_deep_read` action。
7. 新增 `modules/reading/.gitignore`，明确忽略 `workspace/`、`__pycache__/`、PDF、正文抽取、Claude 输出和日志等运行中间产物。
8. 重写 `README.md`，用中文说明独立使用方法、输入输出口径、运行流程、脚本分类和边界约束。

## 当前模块状态

- Reading 已具备独立后端入口：
  `python modules/reading/main.py --action deep-read --article <论文> --claude-mode auto`
- 独立运行产物固定在：
  `modules/reading/workspace/runs/<run-id>/`
- `workspace/` 被 reading 自己的 `.gitignore` 忽略，避免 PDF、全文、prompt、Claude 输出进入 git。
- 旧 TASTE 当前 Find 相关 action 仍通过兼容薄壳保留。

## 环境注意事项

- Python 使用：`/home/fmh/workspace/miniforge/envs/ar_taste/bin/python3.11`
- rg 使用：`/home/fmh/workspace/miniforge/envs/ar_taste/bin/rg`
- Claude/Node 使用：`/home/fmh/workspace/.nvm/versions/node/v22.21.0/bin`
- 不要使用系统默认 `python`；远程默认 `python` 不存在，`python3` 是系统环境，不符合本模块维护要求。

## 本次命令注意事项

- 远程没有默认 `python`；不要用系统 `python3` 做 Reading 维护，统一使用 `/home/fmh/workspace/miniforge/envs/ar_taste/bin/python3.11`。
- `rg` 使用 `/home/fmh/workspace/miniforge/envs/ar_taste/bin/rg`。
- 通过本地 shell 给远程写多行内容时，优先使用：`ssh hidimension_5090_1 "cd /home/fmh/workspace/TASTE && <conda-python> -" <<'PY'`，避免本地 zsh 提前解释引号、反斜杠或变量。

## 后续继续工作前必须先看

1. 先读本文件和 `README.md`，确认模块边界。
2. 只在 `/home/fmh/workspace/TASTE/modules/reading` 下修改 Reading 模块文件。
3. 不要把独立运行产物写到项目根、其它模块、`web/` 或 TASTE 框架目录。
4. 如果需要验证下载/抽取，优先使用 `--claude-mode prepare`，避免无意触发长时间 Claude 调用。
5. 新增脚本必须进功能子目录；根目录只放兼容薄壳。

## 2026-06-18 兼容与边界修复补记

- 补齐 README 中 Reading 模块边界契约原句，满足模块边界测试对 responsibility 的检查。
- 将 `script_manifest.json` 恢复为边界测试期望的顶层脚本清单：`read_pipeline.py`、
  `ensure_current_find_research_plan.py`、`repair_current_find_full_text_evidence.py`；真实实现继续记录在
  `implementation_modules`，不回滚已有子目录结构。
- 删除顶层 `modules/reading/scripts/__init__.py`（功能子目录自己的 `__init__.py` 保留），避免扁平脚本归属测试
  把它误识别成 stage script。
- 修复 `scripts/read_pipeline.py` 兼容薄壳：显式 re-export `_download_pdf`、`_arxiv_pdf_candidates`、
  `_pdf_candidates_for_reading`、`_download_first_readable_pdf`、`_extract_pdf_text`，并把 `run_read` 改为轻代理，
  调用前同步测试/旧调用方 monkeypatch 的私有函数与 `LLMClient` 到真实实现模块。

验证结果：Reading 全量 py_compile 通过；`tests/test_pipeline_mock.py` 23 passed；Finding/Reading 相关边界定向测试
3 passed；Reading contract 与 wrapper 公共面探针通过；Finding/Reading manifest 自检通过；`git diff --check` 通过。
完整 `tests/test_module_boundaries.py` 仍被 Ideation 侧 README/顶层 `scripts/__init__.py` dirty 状态挡住，非本轮
Finding/Reading 修复引入。

## 2026-06-18 多渠道批量测试记录

- 新增 `main.py --action channel-batch-test`，真实实现为 `scripts/pipeline/channel_batch_test.py`。
- 修复 Claude Code 调用方式：`claude -p --add-dir` 的 prompt 必须通过 stdin 传入，不能放在 `--add-dir` 后面；否则会被当作目录并报“Input must be provided”。
- 修复 Nature PDF 链接：Nature/Springer 页面真实 PDF 为 `..._reference.pdf`，不能简单拼 `.pdf`。
- 收紧 HTML 正文判断：会议 virtual poster 页、摘要页、RSS、Crossref 摘要不算全文；HTML 必须有论文正文结构或足够长正文。
- 最终严格批量测试 run：`modules/reading/workspace/batch_tests/channel_batch_20260618_v5_prepare/`。
- 人工核对报告：`modules/reading/workspace/batch_tests/channel_batch_20260618_v5_prepare/manual_audit_v5_zh.md`。
- v5 结果：NIPS2025 10/10、arXiv 10/10、Nature 10/10 通过；ICLR2026 1/10、ICML2026 6/10、Science 系列 4/10、bioRxiv 0/10、SIGKDD2026 0/10 未达标。
- 未达标不是模块伪失败：OpenReview PDF/API 当前环境 403；bioRxiv PDF/HTML/JATS 当前环境 403；SIGKDD2026 官网存在但 accepted/research papers 页面和 DBLP 目录未公开论文；Science.org 部分 PDF 可取但许多 DOI/HTML/PDF 被 403 或不是研究正文。
- 实际 Claude/subagent 小样本：`channel_batch_20260618_claude_sample_v2`，Claude 成功返回 `subagent_deep_read=true` 和 `deep_read_audit`，但对摘要级 Nature 旧抽取诚实标为 `partial_evidence`；说明现在的验收必须继续坚持全文证据，不可用摘要凑精读。

## 2026-06-18 v7 多渠道验收通过记录

- 修复 `main.py` 的运行时导入优先级：Reading 自己的 `scripts/` 和功能子目录优先，避免其它模块的 `scripts/common` 抢占 `common.io`。
- `channel_batch_test.py` 新增 OpenAlex 兜底：ICLR2026、ICML2026 使用会议短语/正式会议名匹配开放 PDF；SIGKDD2026 使用 ACM KDD proceedings DOI 前缀；arXiv 官方 API 429 时使用 OpenAlex 的 ArXiv 源；Science 系列优先 OpenAlex DOI 前缀、Crossref 兜底；bioRxiv 使用官方 published mapping 找对应已发表开放全文。
- 收紧批量验收：`full_text_ok` 必须依赖 `full_text_available=true`，不能只看字符数，防止长的非论文页面或摘要页面误判。
- 完整八渠道验收 run：`modules/reading/workspace/batch_tests/channel_batch_20260618_v7_all_channels/`。命令参数：`--per-channel 10 --candidate-limit 100 --claude-mode prepare --workers 2`。
- v7 总结果：`passed_all_channels`；NIPS2025、ICLR2026、ICML2026、SIGKDD2026、arXiv、bioRxiv、Nature、Science 系列全部达到 10/10。
- 人工核对报告：`modules/reading/workspace/batch_tests/channel_batch_20260618_v7_all_channels/manual_audit_v7_zh.md`，逐渠道核对前 10 个 ready 产物，问题数 0。
- 仍需注意：`prepare` 模式只生成主控 Claude/subagent prompt 和全文证据包，不实际消费 Claude 深读额度；若要验收真实 Claude 输出，应另用小样本 `--claude-mode run`。

## 2026-06-18 Claude/subagent 实跑补记

- 修正 `claude_subagent.py` prompt：明确 `subagent_deep_read` 必须是布尔值，subagent 细节写入 `deep_read_audit`。
- 修正 `channel_batch_test.py` 的 `--claude-mode run` 状态归一：Claude 返回 `completed` 但有 `deep_read_audit.subagent_used=true` 且存在 `reading` 对象时，批测结果归一为 `complete`，避免同一 smoke run 继续误跑后续候选。
- 实际 Claude/subagent smoke run：`modules/reading/workspace/batch_tests/channel_batch_20260618_v7_claude_smoke_v2/`。结果：`passed_all_channels`，单篇 `read_results_status=complete`，`subagent_deep_read=true`（布尔），`deep_read_audit.subagent_used=true`，`reading` 字段齐全。

## 2026-06-18 读后重排与全文健壮性补记

- 当时未接入 Semantic Scholar；该轮只增强 Reading 自身健壮性和读后排序产物。后续已在“Semantic Scholar 可选增强与批量审计补记”中加入可选增强源。
- 新增 `scripts/common/read_ranking.py`：统一根据每篇 reading 产物生成 `read_score`、`read_score_breakdown`、`read_score_audit`、`reading_ranking`、`reading_ranking_order`。若主控 Claude Code 已在逐篇精读后写入 `read_score`，优先采用；否则使用全文证据、精读字段完整度、方法可迁移性、主题贴合和风险控制做确定性保底排序。
- `scripts/pipeline/read_pipeline.py` 旧 Read 流水线在写 `read_results.json`/`read.md` 前会按读后分数重排 readings，并在 Markdown 开头写“读后重评分排序”。
- `scripts/orchestration/ensure_current_find_research_plan.py` 的 current-Find Claude takeover、已验证分片持久化、LLM fallback 三条路径都写入 `reading_ranking` 和 `reading_ranking_order`；`read.md` 的“方法差异、优缺点总览”升级为带排名、读后分、共性优点和共性风险的比较表。
- Claude takeover prompt 已要求主控在全部 Task/subagent 逐篇精读后做跨论文重评分，每个 deep-read fragment 的 `reading` 应写 `read_score`、`read_score_breakdown`、`read_score_audit`。
- 增强 PDF/全文获取：`read_pipeline.py` 增加 DOI/OpenAlex OA PDF 和可选 Unpaywall（需 `UNPAYWALL_EMAIL`）候选；`paper_sources.py` 增加 PMCID 识别和 EuropePMC `fullTextXML` 兜底，Science/PMC PDF 被 403/PoW 拦截时仍可抽取 XML 正文，但仍必须通过正文长度和论文正文结构判断。
- 修复 `main.py`：支持位置式 action（如 `modules/reading/main.py channel-batch-test ...`），并修正 `_ensure_runtime_imports()`，保证 Reading 自己的 `pipeline/common` 包优先于其它 TASTE 模块同名包。
- 验证：全量相关 py_compile 通过；读后排序单元探针通过；`channel_batch_20260618_rerank_smoke_v2` 八渠道 `--per-channel 1 --candidate-limit 12 --claude-mode prepare --workers 2` 结果 `passed_all_channels`；Science 单独 `channel_batch_20260618_rerank_science_smoke` 通过，核对到 `pmc_xml_acquisition.accepted=true`、`full_text_status=full_text_read`、`full_text_chars=104681`。
- 完整 10/渠道严格验收已重跑：`channel_batch_20260618_rerank_v1_all_channels`，命令参数 `--per-channel 10 --candidate-limit 100 --claude-mode prepare --workers 2`，总结果 `passed_all_channels`；8 个渠道均达到 10/10。人工核对报告：`manual_audit_rerank_v1_zh.md`，`problem_count=0`，其中 `full_text_xml_ready_count=4`。

## 2026-06-18 Semantic Scholar 可选增强与批量审计补记

- 新增 `scripts/acquisition/semantic_scholar.py`：作为可选增强源补充 Semantic Scholar Graph 信息，包括 DOI/arXiv/PMCID 等外部 ID、S2 paperId/corpusId、引用数、影响力引用数、TLDR、领域标签和 `openAccessPdf` 候选。
- Semantic Scholar 默认不请求；只有配置 `SEMANTIC_SCHOLAR_API_KEY`/`S2_API_KEY`，或显式设置 `READING_ENABLE_SEMANTIC_SCHOLAR=1` 时才启用。无 key 或 429 不能阻塞 Reading 主流程，也不能把摘要、TLDR 或题录当作全文。
- `paper_sources.py` 的独立单篇精读题录归一会在启用时写入 `semantic_scholar_context` 和获取回执；`read_pipeline.py` 的旧 Read PDF 候选池会接收 `semantic_scholar_open_access_pdf` 候选，但仍必须通过已有 PDF 下载、文件类型和正文抽取校验。
- 新增 `scripts/pipeline/audit_channel_batch.py` 和公开 action `audit-channel-batch`：用于审计多渠道批量验收产物，逐渠道核对 `read_results.json`、`full_text_packet.json`、正文路径边界、`full_text_available`、正文长度，以及 prompt 中的 `Task/subagent` 与 `subagent_deep_read` 标记。
- README 和 `script_manifest.json` 已同步，说明 Semantic Scholar 的可选边界、环境变量、批量审计入口和输出位置。
- 本轮全量全模块功能测试已完成：`py_compile` 覆盖 `modules/reading` 全部 Python 文件；`main.py --contract` 通过；公开 action `deep-read`、`channel-batch-test`、`audit-channel-batch`、`repair-full-text`、`current-find-research-plan` 的帮助入口均可调用。
- `tests/test_pipeline_mock.py` 23 passed；Reading 专门边界用例 `test_finding_reading_full_text_responsibility_boundary` 和 `test_stage_main_contracts_are_callable_without_cli_or_contracts_file` 通过。完整 `tests/test_module_boundaries.py` 仍失败在非 Reading 的既有全仓边界问题，例如 `modules/ideation/README.md` 缺少责任契约原句，以及其它模块仍有 `scripts.*` 导入/根脚本 `__init__.py` 归属问题，本轮未越界修改。
- 独立单篇 smoke：`standalone_semantic_audit_smoke_20260618` 成功取得 arXiv PDF 正文 39512 字，prompt 包含 `Task/subagent` 与 `subagent_deep_read`，路径在 Reading 内。
- Semantic Scholar 显式开启 smoke：`standalone_semantic_enabled_429_smoke_20260618` 在当前无 key 环境下记录 `rate_limited`/429，但仍成功取得 PDF 正文 39512 字并进入 `prepared_for_main_claude_subagent`，证明增强源失败不会阻断主流程。
- 新完整八渠道验收：`channel_batch_20260618_semantic_v1_all_channels`，命令参数 `--per-channel 10 --candidate-limit 100 --claude-mode prepare --workers 2`，总结果 `passed_all_channels`；8 个渠道均达到 10/10。新增审计器输出 `manual_audit_zh.md` / `manual_audit_zh.json`，`problem_count=0`，核对 80 篇，XML 全文兜底 9 篇。
- `git diff --check -- modules/reading` 通过；本轮 workspace 产物经 `git check-ignore` 确认仍被忽略。
