# Experimenting 工作状态

## 2026-06-20 接管整理

- `modules/experimenting_dev` 已替换为正式 `modules/experimenting`，dev 目录已删除。
- 公开入口为 `modules/experimenting/main.py`，`STAGE_NAME=experimenting`，默认 action 为 `run/autonomous_experiment`。
- 独立运行产物默认写 `modules/experimenting/runtime/autonomous`；Web/framework 单阶段调用写 `modules/experimenting/runtime/web/<project>`。
- Web 实验按钮现在走 `web -> framework -> modules/experimenting/main.py`，不再绕回旧 `run_autonomous_research.py` 实验分支。

## 验证

- `python modules/experimenting/main.py --contract` 通过。
- `python -m compileall -q modules/experimenting` 通过。
- root tests 覆盖 web experiment command 的 framework 单阶段桥接。
