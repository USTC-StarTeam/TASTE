# Environment 工作状态

## 2026-06-20 接管整理

- `modules/environment_dev` 已替换为正式 `modules/environment`，dev 目录已删除。
- 公开入口为 `modules/environment/main.py`，`STAGE_NAME=environment`，默认 action 为 `deploy_from_plan`。
- 模块产物只写 `modules/environment/runs/<run_id>/` 和 `modules/environment/latest_decision.json`。
- framework/web 调用时走 `web -> framework -> modules/environment/main.py`，不再使用旧 `run_stage`。
- 当前策略版本来自源码 `DECISION_POLICY_VERSION`；历史 `environment_dev.*` 记录只作为开发历史，不再作为正式口径。

## 验证

- `python modules/environment/main.py --contract` 通过。
- `python -m compileall -q modules/environment` 通过。
- framework 单阶段 dry-run 能生成 `frontend_status.json`，`stage_scope=[environment]`。
